language: python

build_image: htmlgraphic/postfix

env:
  - USER=p08tf1X PASS=p@ssw0Rd

script:
  - bash Docker/Postfix/run.sh &
  - sleep 20
  - /usr/sbin/postconf -n
  - bash Docker/Postfix/build_tests.sh

notifications:
  email:
    recipients:
      - jason@htmlgraphic.com
    on_success: change
    on_failure: always

branches:
  only:
  - master
  - develop